_BASE_: "Base-RetinaNet.yaml"

MODEL:
  RESNETS:
    DEPTH: 50
  RETINANET:
    NUM_CLASSES: 1
  BACKBONE:
    FREEZE_AT: 2

SOLVER:
  STEPS: (30000,)
  MAX_ITER: 20000
  CHECKPOINT_PERIOD: 10000
  IMS_PER_BATCH: 2
  BASE_LR: 0.0002
  WEIGHT_DECAY: 0.001

DATALOADER:
  NUM_WORKERS: 0

INPUT:
  MIN_SIZE_TRAIN: (0,)
  MIN_SIZE_TEST: 0

TEST:
  EVAL_PERIOD: 10000

